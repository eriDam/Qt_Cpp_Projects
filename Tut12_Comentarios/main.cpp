/*
 * Tut12 Coment:
 *
 * https://www.youtube.com/watch?v=QltvjKnpO4c
 *
 * Aplicación que enseña los comentarios
 */
#include <iostream>

using namespace std;

int main()
{
    //Tipo de comentario una sola linea sin cerrar
    /*Tipo de varias lineas o una linea cerrado*/

    //Para que cierto codigo no se cumpla pero no lo queremos borrar

    /*
    //Colocamos el switch y entre parentesis cual va a ser la que está cambiando que es c
    switch(c)
    {
    //Para todos estos casos será el mismo código, mejor que hacer un if, será menos código
    case 'a':
    case 'e':
    case 'i':
    case 'o':
    case 'u':
        cout << "Es una vocal \n " << endl;
        break;//para que no siga ejecuntado este code, si no hay otro caso pasará al siguiente caso o default
    default:
        cout << "Es una consonante" << endl;
    }
    */

}
